#include <iostream>
using namespace std;

int main() {
    int age;
    count << "Enter your age: ";
    cin >> age;

    if(age >= 18) {
        count << "You are an adult." << endl;
    } else {
        count << "You are a minor" << endl;
    }
    return 0;
}