#include <stdio.h
#include <math.h>

int main() {
    double base = 2.0, expoenent = 3.0;
    double result;

    resutl = pow(base, exponent);
    printf("%.2f ^ %.2f = %.2f\n", base, exponent, result);
    return 0;
}